(l4)=
# L4

The altimeter L4 products are monthly gridded (1°x1°) significant wave height 
statistics files, processed from each mission L2P series.

It provides statistics computed from `swh_denoised` variable in L2P (SWH adjusted
and denoised), retaining only the good quality measurements within these L2P 
files. All missions processed as L2P and adjusted to each other are included and
merged together within these monthly statistics.

For altimeters, only the Ku band measurements are considered, when available 
(the only current exception being SARAL for which only Ka band is provided).

This section describes in detail the specific content of the {{cci_version}} L4 
for Sea State CCI, configured as shown in the table 
{numref}`l4_content_summary`, which can be used to locate appropriate 
information in this document. 

```{table} Summary description of the contents of a CCI Sea State L4 data product
:name: l4_content_summary

| netCDF File Contents | Description                                                                                               |
|----------------------|-----------------------------------------------------------------------------------------------------------|
| **Coordinate variables**  | Information to permit locating data on orthogonal grids, as defined in {numref}`coordinate_variables` | 
| **Statistical data record variables** | significant wave height statistics for 1st band altimeter (usually Ku) as defined in {numref}`l4_variables_statistics`                                          |                                         | 
| **Global Attributes**  | A collection of required global attributes describing general characteristics of the file, as defined in section {numref}`global_attributes`  |
```


(l4_variables_statistics)=
## L2P geophysical data record format specification
The {numref}`table_l4_variables_statistics` provides an overview of the CCI 
Sea State 4 significant wave height statistics data record within a L4 file. In the 
following sections, each variable within the L4 data file is described in detail.

```{table} Summary description of CCI Sea State L4 SWH statistics records
:name: table_l4_variables_statistics

| Variable Name          | Description           | Units  |
|-----------------------|------------------------|--------|
| [swh_mean](__l4_swh_mean) | Mean of median significant wave height values | m |
| [swh_max](__l4_swh_max) | Maximum median significant wave height value  | m |
| [swh_rms](__l4_swh_rms) | RMS of median significant wave height values | m |
| [swh_num](__l4_swh_num) | Number of median significant wave height values | 1 |
| [swh_sum](__l4_swh_sum) | Total of median significant wave height values | m |
| [swh_squared_sum](__l4_swh_squared_sum) | Total of median significant wave height squared values | m2 |
| [swh_log_sum](__l4_swh_log_sum) | Total of median significant wave height log values | m |
| [swh_log_squared_sum](__l4_swh_log_squared_sum) | Total of median significant wave height log squared values | m2 |
| [swh_num_gt0050](__l4_swh_num_gt0050) | Number of median significant wave height values greater than 0.5 m  | 1 |
| [swh_num_gt0100](__l4_swh_num_gt0100) | Number of median significant wave height values greater than 1.0 m  | 1 |
| [swh_num_gt0150](__l4_swh_num_gt0150) | Number of median significant wave height values greater than 1.5 m  | 1 |
| [swh_num_gt0200](__l4_sswh_num_gt0200) | Number of median significant wave height values greater than 2 m  | 1 |
| [swh_num_gt0250](__l4_swh_num_gt0250) | Number of median significant wave height values greater than 2.5 m  | 1 |
| [swh_num_gt0300](__l4_swh_num_gt0300) | Number of median significant wave height values greater than 3.0 m  | 1 |
| [swh_num_gt0350](__l4_swh_num_gt0350) | Number of median significant wave height values greater than 3.5 m  | 1 |
| [swh_num_gt0400](__l4_swh_num_gt0400) | Number of median significant wave height values greater than 4.0 m  | 1 |
| [swh_num_gt0500](__l4_swh_num_gt0500) | Number of median significant wave height values greater than 5.0 m  | 1 |
| [swh_num_gt0600](__l4_swh_num_gt0600) | Number of median significant wave height values greater than 6.0 m  | 1 |
| [swh_num_gt0800](__l4_swh_num_gt0800) | Number of median significant wave height values greater than 8.0 m  | 1 |
| [swh_num_gt1000](__l4_swh_num_gt1000) | Number of median significant wave height values greater than 10.0 m  | 1 |
```

(__l4_swh_mean)=
### `swh_mean`

The **mean significant wave height (SWH)**, within 1°x1° grid cells, averaged 
from all 1 Hz resolution SWH measurements by all available missions processed in 
L2P, adjusted and denoised (`swh_denoised` variable). Only good quality 
measurements (`quality_level` equal to 3 in L2P files).

For ERS-1, ERS-2, TOPEX, Sentinel-3 A & B and Sentinel-6, the 1 Hz 
measurements were estimated from the full resolution SWH measurements 
provided in the source Agency’s GDR & SGDR products. Refer to the processing 
details {numref}`__whales` for the specific source used for these missions.

For Jason-1, Jason-2, Jason-3, Envisat, SARAL and CryoSat-2, a specific 
retracking was performed, using the WHALES nadir altimetry retracker 
selected by the CCI Sea State experts. For more details on the WHALES 
retracker, refer to the processing details {numref}`__whales`.

The `swh_mean` variable in a L4 product follows the format shown in table {numref}`l4_swh_mean`.


```{table} CDL example description of **<span style="font-family:courier;">swh_mean</span>** variable
:name: l4_swh_mean

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_mean`     | m (meter) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_mean

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_mean[(,:]'| sed 's/[[:space:]]//'"
```


(__l4_swh_max)=
### `swh_max`


```{table} CDL example description of **<span style="font-family:courier;">swh_max</span>** variable
:name: l4_swh_max

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_max`     | m (meter) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_max

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_max[(,:]'| sed 's/[[:space:]]//'"
```


(__l4_swh_num)=
### `swh_num`

```{table} CDL example description of **<span style="font-family:courier;">swh_num</span>** variable
:name: l4_swh_num

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_num`     | 1 (count) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_num

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_num[(,:]'| sed 's/[[:space:]]//'"
```


(__l4_swh_sum)=
### `swh_sum`


```{table} CDL example description of **<span style="font-family:courier;">swh_sum</span>** variable
:name: l4_swh_sum

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_sum`     | m (meter) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_sum

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_sum[(,:]'| sed 's/[[:space:]]//'"
```

(__l4_swh_squared_sum)=
### `swh_squared_sum`

```{table} CDL example description of **<span style="font-family:courier;">swh_squared_sum</span>** variable
:name: l4_swh_squared_sum

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_squared_sum`     | m (meter) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_squared_sum

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_squared_sum[(,:]'| sed 's/[[:space:]]//'"
```

(__l4_swh_log_sum)=
### `swh_log_sum`


```{table} CDL example description of **<span style="font-family:courier;">swh_log_sum</span>** variable
:name: l4_swh_log_sum

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_log_sum`     | m (meter) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_log_sum

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_log_sum[(,:]'| sed 's/[[:space:]]//'"
```

(__l4_swh_log_squared_sum)=
### `swh_log_squared_sum`


```{table} CDL example description of **<span style="font-family:courier;">swh_log_squared_sum</span>** variable
:name: l4_swh_log_squared_sum

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_log_squared_sum`     | m2 (square meter) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_log_squared_sum

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_log_squared_sum[(,:]'| sed 's/[[:space:]]//'"
```

(__l4_swh_num_gt0050)=
### `swh_num_gt0050`


```{table} CDL example description of **<span style="font-family:courier;">swh_num_gt0050</span>** variable
:name: l4_swh_num_gt0050

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_num_gt0050`     | 1 (count) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_num_gt0050

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_num_gt0050[(,:]'| sed 's/[[:space:]]//'"
```

(__l4_swh_num_gt0100)=
### `swh_num_gt0100`

```{table} CDL example description of **<span style="font-family:courier;">swh_num_gt0100</span>** variable
:name: l4_swh_num_gt0100

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_num_gt0100`     | 1 (count) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_num_gt0100

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_num_gt0100[(,:]'| sed 's/[[:space:]]//'"
```

(__l4_swh_num_gt0150)=
### `swh_num_gt0150`

```{table} CDL example description of **<span style="font-family:courier;">swh_num_gt0150</span>** variable
:name: l4_swh_num_gt0150

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_num_gt0150`     | 1 (count) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_num_gt0150

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_num_gt0150[(,:]'| sed 's/[[:space:]]//'"
```

(__l4_swh_num_gt0200)=
### `swh_num_gt0200`

```{table} CDL example description of **<span style="font-family:courier;">swh_num_gt0200</span>** variable
:name: l4_swh_num_gt0200

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_num_gt0200`     | 1 (count) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_num_gt0200

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_num_gt0200[(,:]'| sed 's/[[:space:]]//'"
```

(__l4_swh_num_gt0250)=
### `swh_num_gt0250`

```{table} CDL example description of **<span style="font-family:courier;">swh_num_gt0250</span>** variable
:name: l4_swh_num_gt0250

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_num_gt0250`     | 1 (count) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_num_gt0250

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_num_gt0250[(,:]'| sed 's/[[:space:]]//'"
```


(__l4_swh_num_gt0300)=
### `swh_num_gt0300`

```{table} CDL example description of **<span style="font-family:courier;">swh_num_gt0300</span>** variable
:name: l4_swh_num_gt0300

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_num_gt0300`     | 1 (count) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_num_gt0300

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_num_gt0300[(,:]'| sed 's/[[:space:]]//'"
```

(__l4_swh_num_gt0350)=
### `swh_num_gt0350`

```{table} CDL example description of **<span style="font-family:courier;">swh_num_gt0350</span>** variable
:name: l4_swh_num_gt0350

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_num_gt0350`     | 1 (count) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_num_gt0350

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_num_gt0350[(,:]'| sed 's/[[:space:]]//'"
```

(__l4_swh_num_gt0400)=
### `swh_num_gt0400`

```{table} CDL example description of **<span style="font-family:courier;">swh_num_gt0400</span>** variable
:name: l4_swh_num_gt0400

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_num_gt0400`     | 1 (count) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_num_gt0400

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_num_gt0400[(,:]'| sed 's/[[:space:]]//'"
```


(__l4_swh_num_gt0500)=
### `swh_num_gt0500`

```{table} CDL example description of **<span style="font-family:courier;">swh_num_gt0500</span>** variable
:name: l4_swh_num_gt0500

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_num_gt0500`     | 1 (count) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_num_gt0500

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_num_gt0500[(,:]'| sed 's/[[:space:]]//'"
```


(__l4_swh_num_gt0600)=
### `swh_num_gt0600`

```{table} CDL example description of **<span style="font-family:courier;">swh_num_gt0600</span>** variable
:name: l4_swh_num_gt0600

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_num_gt0600`     | 1 (count) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_num_gt0600

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_num_gt0600[(,:]'| sed 's/[[:space:]]//'"
```

(__l4_swh_num_gt0800)=
### `swh_num_gt0800`


```{table} CDL example description of **<span style="font-family:courier;">swh_num_gt0800</span>** variable
:name: l4_swh_num_gt0800

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_num_gt0800`     | 1 (count) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_num_gt0800

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_num_gt0800[(,:]'| sed 's/[[:space:]]//'"
```

(__l4_swh_num_gt1000)=
### `swh_num_gt1000`


```{table} CDL example description of **<span style="font-family:courier;">swh_num_gt1000</span>** variable
:name: l4_swh_num_gt1000

| **Storage type**  | **Name**  | **Unit** |
|-------------------|-----------|----------|
| float             | `swh_num_gt1000`     | 1 (count) |
```

```{code-cell}
:tags: [remove-input]
:name: l4_swh_num_gt1000

!bash -c "ncdump -h ../samples/ESACCI-SEASTATE-L4-SWH-MULTI_1M-202301-fv01.nc | grep $'[ , \t]swh_num_gt1000[(,:]'| sed 's/[[:space:]]//'"
```

